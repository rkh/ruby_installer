#!/bin/bash
if [ -f $RUBY_PATH/bin/$1 ]; then exit; fi
cat > $RUBY_PATH/bin/$1 <<EOS
#!/bin/bash
# generated by $0
PATH=\$RUBY_PATH/\$RUBY_VERSION/bin:\$PATH exec \$RUBY_PATH/\$RUBY_VERSION/bin/$1 \$@
EOS
chmod +x $RUBY_PATH/bin/$1
